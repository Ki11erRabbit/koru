[package]
name = "koru-core"
version = "0.1.0"
edition = "2024"

[dependencies]
mlua = { workspace = true, features = ["module", "send", "async"]}
tokio = { version = "1.47.1", features = ["full"] }

[features]
default = ["luajit"]
lua51 = ["mlua/lua51"]
luajit = ["mlua/luajit"]